# ============================================================
# ORACLE-TMF: Observational Reasoning and Coercive Analysis
#             for Latent Evolution — Temporal Mutation Forecaster
# ============================================================
# Install: python -m venv .venv && .venv/Scripts/activate && pip install -r requirements.txt

# ── Core Android Analysis ──────────────────────────────────
androguard==3.4.0a1        # DEX disassembly, CFG, Smali, manifest parsing
networkx==3.4.2            # Graph algorithms (BFS, CFG reachability)

# ── LLM & Agent Orchestration ─────────────────────────────
anthropic==0.39.0          # Claude API (Sonnet for agents)
langchain==0.3.14          # Agent chaining framework
langchain-anthropic==0.3.1 # LangChain <-> Anthropic bridge

# ── Vector DB & RAG ───────────────────────────────────────
chromadb==0.5.23           # Persistent vector store for MITRE + MalNet RAG
sentence-transformers==3.3.1  # Sentence-BERT for RDG resolution & semantic similarity

# ── ML / Classification ───────────────────────────────────
xgboost==2.1.3             # DTE 4-class dormancy classifier (300 estimators)
scikit-learn==1.6.1        # Feature pipelines, train/test split, metrics
numpy==1.26.4              # Numerical computation
scipy==1.14.1              # Statistical distributions for Bayesian scoring

# ── Cryptography & File Handling ──────────────────────────
cryptography==44.0.0       # PKCS7 APK certificate parsing (Stage A)
python-magic==0.4.27       # MIME type detection (anti-analysis check)

# ── XML Safety ────────────────────────────────────────────
defusedxml==0.7.1          # Safe XML parsing for attacker-controlled APK resources

# ── Fuzzy Hashing ─────────────────────────────────────────
# ssdeep>=3.4              # Context-triggered piecewise hash (install separately: apt-get install libfuzzy-dev)
ppdeep==20260221           # Alternative pure-python implementation used if ssdeep is unavailable

# ── Output Generation ─────────────────────────────────────
fpdf2==2.8.2               # PDF brief generation (Stage L)
stix2==3.0.1               # STIX 2.1 threat intelligence bundle
PyYAML==6.0.2              # YAML serialization for YARA rules + configs

# ── API Layer ─────────────────────────────────────────────
fastapi==0.115.6           # REST API framework
uvicorn==0.34.0            # ASGI server for FastAPI
pydantic==2.10.4           # Request/response schema validation

# ── Frontend Dashboard ────────────────────────────────────
streamlit==1.45.0          # Rapid prototyping dashboard
plotly==5.24.1             # Interactive charts (gauge, timeline, treemap)
pandas==2.2.3              # Tabular data handling for dashboard tables

# ── XML / AXML Parsing ────────────────────────────────────
lxml==5.4.0                # Manifest XML parsing (fallback)

# ── Utility ───────────────────────────────────────────────
python-dateutil==2.9.0     # Date parsing for versioned APK ordering
tqdm==4.67.1               # Progress bars for pipeline stages
colorlog==6.9.0            # Coloured logging output

# ── Testing ───────────────────────────────────────────────
pytest>=8.0.0              # Unit testing framework
